import { EventPopupButton } from "../../typings/common";
export declare const TELEGRAM_POPUP_BUTTON: {
    readonly TYPES: {
        readonly DEFAULT: "default";
        readonly OK: "ok";
        readonly CLOSE: "close";
        readonly CANCEL: "cancel";
        readonly DESTRUCTIVE: "destructive";
    };
    readonly MAX_ID_LENGTH: 64;
    readonly MAX_TEXT_LENGTH: 64;
};
export declare class WebAppPopupButton {
    #private;
    constructor(config: EventPopupButton | WebAppPopupButton);
    get data(): EventPopupButton;
}
