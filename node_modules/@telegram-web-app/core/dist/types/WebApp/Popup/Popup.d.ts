import { PopupParams } from "../../typings/WebApp";
import { Nullable } from "../../typings/utils";
import { OpenPopupEventData } from "../../typings/WebView";
export type PopupCallback = (id: string) => any;
interface OpenPopupOptions {
    params: PopupParams;
    callback?: Nullable<PopupCallback>;
}
export declare const TELEGRAM_POPUP: {
    readonly MAX_TITLE_LENGTH: 64;
    readonly MAX_MESSAGE_LENGTH: 256;
    readonly MIN_BUTTONS: 1;
    readonly MAX_BUTTONS: 3;
};
export declare class Popup {
    #private;
    open({ params: { title, message, buttons }, callback }: OpenPopupOptions): void;
    close(): void;
    get isOpened(): boolean;
    get params(): OpenPopupEventData | null;
    get callback(): PopupCallback | null;
}
export {};
