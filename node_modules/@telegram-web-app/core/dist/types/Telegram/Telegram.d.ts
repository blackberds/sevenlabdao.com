import { Nullable } from "../typings/utils";
import { WebView } from "../typings/WebView";
import { WebApp } from "../typings/WebApp";
export interface TelegramOptions {
    exposeInMainWorld?: Nullable<boolean>;
}
export interface Telegram {
    WebApp: WebApp;
    WebView: WebView;
}
export declare class TelegramWebAppContainer implements Telegram {
    #private;
    constructor({ exposeInMainWorld }?: TelegramOptions);
    get WebApp(): WebApp;
    get WebView(): WebView;
}
