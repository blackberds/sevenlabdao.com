export type Initializer<ThisArg> = (this: ThisArg) => void;
export declare class Initializers<ThisArg> {
    #private;
    add(initializer: Initializer<ThisArg>): void;
    invoke(instance: ThisArg): void;
}
